Directory meant to contain one or more Omnistat databases under different
subdirectories. Databases in this directory are imported into the main
database when launching the Docker Compose environment.
